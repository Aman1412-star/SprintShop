const express = require("express");
const router = express.Router();

router.post('/:cart/:product_id/product', (req,res)=>{

}) 

router.get('/:cart/:cart_id', (req,res)=>{

})

router.delete('/:cart/delete', (req,res)=>{

}) 
module.exports = router;