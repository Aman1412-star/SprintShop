const express = require("express");
const router = express.Router();

router.post('/address', (req,res)=>{

})

router.get('/address/:address_id', (req,res)=>{

})

router.put('/address/edit', (req,res)=>{

})

module.exports = router;